# Database
DB_PASSWORD=changeme
POSTGRES_DB=athena
POSTGRES_USER=athena

# Service hosts (Docker internal names — change only if running outside Docker)
OLLAMA_HOST=ollama
OLLAMA_PORT=11434
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Auth
JWT_SECRET_KEY=supersecretkey-change-in-production-to-random-64-char-string
SEED_ADMIN_PASSWORD=athena

# Frontend — direct backend URL for SSE streaming (bypasses Next.js rewrite proxy buffering)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# App config
LOG_LEVEL=INFO
OLLAMA_MODEL=qwen2.5:7b

# Frontend — model name shown in chat UI (must match OLLAMA_MODEL)
NEXT_PUBLIC_OLLAMA_MODEL=qwen2.5:7b

# Optional external services (leave empty to disable)
SERP_API_KEY=

# MCP (all optional)
JELLYFIN_URL=
JELLYFIN_API_KEY=
HOME_AUTO_URL=
SEAFILE_URL=
SEAFILE_API_KEY=
